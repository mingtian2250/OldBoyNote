
## 安装
http://repo.saltstack.com

master安装：
+ sudo yum install salt-master
+ sudo yum install salt-minion

minion安装：
+ sudo yum install salt-minion

启动master
```bash
systemctl start salt-master
```
编辑 /etc/salt/minion
```bash
# 填写ip或主机名，主机名必须支持DNS解析
master: 192.168.60.32
```
